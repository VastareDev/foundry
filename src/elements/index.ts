/**
 * @file Barrel export Foundry exports.
 * @description
 * Centralises and re-exports all elemen level modules. (e.g. html, body, paragraph, etc.)
 * to provide a unified import interface for consumers.
 *
 * Example Usage:
 * - import * as elements from "@vastare/foundry/elements";
 * - import { bodyClasses } from "@vastare/foundry/elements/body";
 *
 * This pattern ensures consistent, tree shakable access to individual elements.
 */

//export * as anchor from "./anchor";
//export * as article from "./paragraph";
console.log('test');
